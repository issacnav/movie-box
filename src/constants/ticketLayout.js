/** Keep loading skeleton and digital ticket the same footprint on screen. */
export const TICKET_MAX_WIDTH_CLASS = 'max-w-[220px]'
/** width / height — matches digital ticket `article` (200×520 design units). */
export const TICKET_ASPECT_RATIO_CLASS = 'aspect-[200/520]'
